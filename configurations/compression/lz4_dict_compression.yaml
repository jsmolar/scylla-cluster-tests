post_prepare_cql_cmds: "CREATE KEYSPACE IF NOT EXISTS keyspace1 WITH replication = {'class': 'org.apache.cassandra.locator.NetworkTopologyStrategy', 'replication_factor': '3'} AND durable_writes = true AND tablets = {'enabled': true} AND compression = {'sstable_compression': 'LZ4WithDictsCompressor'};"
